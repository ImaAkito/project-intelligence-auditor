# Readmission Risk Model

This project predicts readmission from tabular encounter features. The dataset contains multiple visits per patient under the `patient_id` column. The reported validation AUC is high and the current README describes the model as ready for deployment.

The auditor is expected to inspect the evaluation methodology rather than accepting the metric at face value.
